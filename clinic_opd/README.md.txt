For the usage of Front end 
-> download the project from github
-> open the folder in vsCode
-> navigate to the project directory (Inside the project) in the vs code terminal
-> run a command npm install
-> this downloads the node modules
-> run npm install to run the project
